{% macro AzureAccountSpec() -%}

title: AzureAccountSpec
type: object
x-calm-dsl-type: azure_account_spec
properties:
  subscription_id:
    type: string
  tenant_id:
    type: string
  client_id:
    type: string
  cloud_environment:
    type: string
    default: PublicCloud
    enum: ["PublicCloud", "GermanCloud", "ChinaCloud", "USGovernmentCloud"]
  client_key:
    type: string

{%- endmacro %}


{% macro AzureAccountSpecSchema() -%}

AzureAccountSpec:
  {{ AzureAccountSpec()|indent(2) }}

{%- endmacro %}
