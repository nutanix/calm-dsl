{%- macro simple_secret_time_var(obj) -%}
{%- if obj.regex %}
{{obj.name}} = CalmVariable.Simple.Secret.time('{{obj.value}}', label='{{obj.label}}',regex='{{obj.regex}}', validate_regex={{obj.validate_regex}})
{%- else %}
{{obj.name}} = CalmVariable.Simple.Secret.time('{{obj.value}}', label='{{obj.label}}')
{%- endif %}
{%- endmacro %}

{{ simple_secret_time_var(obj) }}
