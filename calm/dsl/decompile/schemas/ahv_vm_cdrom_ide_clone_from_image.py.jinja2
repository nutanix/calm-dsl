{%- macro ahv_vm_cdrom_ide_clone_from_image(obj) -%}
{% if obj.bootable and obj.size %}
AhvVmDisk.CdRom.Ide.cloneFromImageService("{{obj.name}}", bootable=True, disk_size_mib={{obj.size}})
{% elif obj.bootable %}
AhvVmDisk.CdRom.Ide.cloneFromImageService("{{obj.name}}", bootable=True)
{% elif obj.size %}
AhvVmDisk.CdRom.Ide.cloneFromImageService("{{obj.name}}", disk_size_mib={{obj.size}})
{% else %}
AhvVmDisk.CdRom.Ide.cloneFromImageService("{{obj.name}}")
{% endif %}
{% endmacro %}

{{ ahv_vm_cdrom_ide_clone_from_image(obj) }}
