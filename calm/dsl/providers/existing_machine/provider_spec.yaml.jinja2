
{% macro EMCreateSpec() -%}
title: Existing Machine CreateSpec
type: object
properties:
  type:
    type: string
  address:
    type: string

{%- endmacro %}


components:
  schemas:
    provider_spec:
      {{ EMCreateSpec() | indent(6) }}
